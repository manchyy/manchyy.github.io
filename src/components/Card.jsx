const Card = () => {
  const cardContainer = {
    // fontSize: "10px",
    borderStyle: "solid",
    borderWidth: "2px solid",
  };

  return (
    <div style={cardContainer}>
      <p>Test :D</p>
    </div>
  );
};

export default Card;
